#include <bits/stdc++.h>
#include <iostream>
using namespace std;
int main(){

ios_base::sync_with_stdio(0);
cin.tie(0);
    int T;
    cin >> T;
    unsigned long long int a,b,s;
    for (int i = 0; i < T; i++){
        cin >> a;
        cin >> b;
        cout << a+b << endl;
    }
    return 0;
}
